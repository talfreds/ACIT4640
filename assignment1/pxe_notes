path of files: /var/www/lighttpd


lots of options required for pxe server, accessed via http


TODO:
ssh key setup

sudo rules (passwordless)

admin account --> in wheel group


for config files/script: put them in the files folder with ks.cfg on the pxe server
--> make them available to the new machine
--> curl them from the new machine in the end of the ks.cfg file

